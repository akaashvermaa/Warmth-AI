# Core runtime
Flask>=2.3.0
Flask-Cors>=4.0.0
Flask-Limiter>=3.0.0

# NLP / Sentiment
vaderSentiment>=3.3.2
nltk>=3.8.0

# Vector search & embeddings
sentence-transformers>=2.2.0

# Optional LLM runtime (install only if you have Ollama locally)
# ollama>=0.1.0

# Data & processing
numpy>=1.21.0
scikit-learn>=1.0.0
pandas>=1.3.0

# Security & utils
cryptography>=41.0.0
PyJWT>=2.8.0
python-dotenv>=0.19.0
requests>=2.28.0

# Dev/test
pytest>=7.0.0
#ruff>=0.24.0

# Notes:
# - SQLite3 and python's logging are part of the standard library and
#   should not be listed as pip packages. If you plan to run an LLM
#   backend locally, install `ollama` separately per its instructions.
